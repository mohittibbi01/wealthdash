/* ── t215: Router Notes ─────────────────────────────────────────────────
 *
 * Task t215 (Stocks — CAGR, Gain%, Cost Basis per stock) does NOT add
 * any new routes to api/router.php.
 *
 * All t215 output (cagr, gain_pct_live, years_held, avg_buy_price,
 * gain_type) is returned by the existing route:
 *
 *   GET /api/?action=stocks_list&type=holdings
 *       → api/stocks/stocks_list.php  (updated with t215 SQL columns)
 *
 * Existing routes confirmed untouched:
 *   stocks_list            → api/stocks/stocks_list.php
 *   stocks_add             → api/stocks/stocks_add.php
 *   stocks_delete          → api/stocks/stocks_delete.php
 *   stocks_search          → api/stocks/stocks_search.php
 *   stocks_price           → api/stocks/stocks_price.php
 *   stocks_refresh_prices  → api/stocks/stocks_refresh_prices.php
 *   stocks_fundamentals    → api/stocks/fundamentals.php
 *
 * ── End t215 Router Notes ─────────────────────────────────────────── */
