========================================================
ROUTER NOTES — ID-W5 Session 6 (5 Tasks)
t329, t330, t331, t332, t333
========================================================

── t329: AI Weekly Digest ──
  csrfExempt: 'ai_digest_history'
  case 'ai_weekly_digest':
  case 'ai_digest_history':
      require APP_ROOT . '/api/ai/weekly_digest.php'; exit;

── t330: AI Chatbot ──
  csrfExempt: 'ai_chat_history', 'ai_chat_sessions'
  case 'ai_chat_send':
  case 'ai_chat_history':
  case 'ai_chat_sessions':
  case 'ai_chat_clear':
      require APP_ROOT . '/api/ai/chatbot.php'; exit;

── t331: AI SIP Optimizer ──
  csrfExempt: 'ai_sip_optimize'
  case 'ai_sip_optimize':
      require APP_ROOT . '/api/ai/sip_optimizer.php'; exit;

── t332: AI Goal Advisor ──
  csrfExempt: 'ai_goal_advice'
  case 'ai_goal_advice':
      require APP_ROOT . '/api/ai/goal_advisor.php'; exit;

── t333: AI Portfolio Report Card ──
  csrfExempt: 'ai_report_card_history'
  case 'ai_report_card':
  case 'ai_report_card_history':
      require APP_ROOT . '/api/ai/report_card.php'; exit;

========================================================
SIDEBAR NOTES (templates/sidebar.php):
  AI section mein add karo:
    'ai_weekly_digest'  => APP_ROOT . '/pages/ai/weekly_digest.php'
    'ai_chatbot'        => APP_ROOT . '/pages/ai/chatbot.php'
    'ai_sip_optimizer'  => APP_ROOT . '/pages/ai/sip_optimizer.php'
    'ai_goal_advisor'   => APP_ROOT . '/pages/ai/goal_advisor.php'
    'ai_report_card'    => APP_ROOT . '/pages/ai/report_card.php'

========================================================
OVERWRITE RULES:
  t330: api/ai/chatbot.php   → OVERWRITE existing chatbot.php
  All other files             → NEW (no conflict)
========================================================
MIGRATION ORDER:
  1. t329_migration.sql  → ai_weekly_digests table
  2. t330_migration.sql  → ai_chat_history table
  3. t331_migration.sql  → rate_limit_buckets (IF NOT EXISTS safe)
  4. t332_migration.sql  → rate_limit_buckets (IF NOT EXISTS safe)
  5. t333_migration.sql  → ai_portfolio_reviews table
========================================================

========================================================
COMBINED HANDOFF SUMMARY — ID-W5 Session 6 (5 Tasks)
========================================================

✅ t329 — AI Weekly Digest
   api/ai/weekly_digest.php + pages/ai/weekly_digest.php + migration
   DB: ai_weekly_digests (user_id, week_key ISO, digest_json)
   Notes:
     - Weekly cache (1 per ISO week per user), regenerate button
     - Claude AI: portfolio update + market highlights + 3 action items + motivation
     - Rule-based fallback: auto-generated without API key
     - History modal (last 8 weeks)
     - Gradient hero card with stats row

✅ t330 — AI Chatbot (Ask About Investments)
   api/ai/chatbot.php + pages/ai/chatbot.php + migration
   DB: ai_chat_history (user_id, role, message, context_id)
   ⚠️ OVERWRITE existing api/ai/chatbot.php
   Notes:
     - Multi-turn conversation with portfolio context injected in system prompt
     - Hinglish responses, Indian financial context
     - Rule-based fallback (keyword-based) when API key not set
     - Quick question buttons sidebar (7 common questions)
     - New chat / clear session support
     - Enter to send, Shift+Enter for newline

✅ t331 — AI SIP Optimizer (Smart Allocation)
   api/ai/sip_optimizer.php + pages/ai/sip_optimizer.php + migration (rate_limit_buckets only)
   DB: No new tables — reads mf_sips, mf_holdings, finance_profiles
   Notes:
     - Category guess from fund name (no mf_funds table dependency)
     - Current vs ideal allocation comparison table
     - Step-up suggestion (10% annual)
     - Savings rate check vs 20% income target
     - AI narrative via Claude + rule-based gap analysis always computed

✅ t332 — AI Goal Advisor (Smart Path to Goals)
   api/ai/goal_advisor.php + pages/ai/goal_advisor.php + migration (rate_limit_buckets only)
   DB: No new tables — reads goals, goal_checkins, mf_sips, mf_holdings, finance_profiles
   Notes:
     - Per-goal: progress%, time_progress%, variance, months_left, sip_needed
     - Total SIP gap across all goals
     - Status cards: on_track / slightly_behind / behind / completed
     - AI narrative via Claude for holistic advice
     - Depends: tg005_migration.sql (goal_checkins) must run first

✅ t333 — AI Portfolio Report Card (Monthly Grade)
   api/ai/report_card.php + pages/ai/report_card.php + migration
   DB: ai_portfolio_reviews (user_id, review_month, review_type, grade, score, strengths JSON, weaknesses JSON, actions JSON)
   Notes:
     - 5 scoring components: Returns(25) + Diversification(20) + SIP(20) + Goals(20) + Profile(15) = 100
     - Grades: A+ to D with color-coded ring (canvas arc)
     - Monthly cache with force regenerate option
     - AI: Claude enriches strengths/weaknesses/actions
     - Rule-based: always computed, AI optional
     - History modal (last 12 months)
     - Shared table with portfolio_review.php (review_type='report_card')

========================================================
DEPENDENCY CHAIN:
  t329 → mf_holdings, mf_nav_latest, mf_sips
  t330 → mf_holdings, mf_nav_latest, mf_sips (no goals needed)
  t331 → mf_sips, mf_holdings, finance_profiles (ti001)
  t332 → goals, goal_checkins (tg005), mf_sips, finance_profiles (ti001)
  t333 → mf_holdings, mf_nav_latest, mf_sips, goals, finance_profiles (ti001)

All 5 tasks work WITHOUT Anthropic API key (rule-based fallback).
Set ANTHROPIC_API_KEY in .env for full AI features.
========================================================
