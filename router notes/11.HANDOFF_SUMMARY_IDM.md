# HANDOFF SUMMARY — Worker ID-M (mohit87jain87@gmail.com)
## Module: Admin · Dedup · Validation · REST API
## Session: t211 + t479 + t480 + t504
## Status: ✅ ALL 4 TASKS DONE

---

## t211 — Admin DB Backup & Restore ✅
**File:** `api/admin/db_backup.php`
**Migration:** `database/migrations/t211_migration.sql`

**Tables created:**
- `db_backups` — backup records (filename, size, status, created_by)
- `db_restore_log` — restore audit trail

**Actions implemented:**
| Action | Type | Description |
|--------|------|-------------|
| `admin_db_backup_list` | GET | List all backups with file_exists check |
| `admin_db_backup_stats` | GET | Total count, last backup, disk used |
| `admin_db_backup_create` | POST | Run mysqldump, supports gzip compression |
| `admin_db_backup_delete` | POST | Delete DB record + file from disk |
| `admin_db_backup_download` | GET | Streams file as download (ob_end_clean) |
| `admin_db_restore_upload` | POST | Upload .sql/.sql.gz to backups/ dir |
| `admin_db_restore_run` | POST | Run mysql restore, requires `confirmed=true` |
| `admin_db_restore_log` | GET | Last 30 restore runs |

**Notes:**
- mysqldump / mysql auto-detected from common server paths
- Backup dir: `APP_ROOT/backups/` with `.htaccess` protection
- Restore requires `confirmed=true` to prevent accidents
- All actions admin-only (403 otherwise)

---

## t479 — Duplicate Transaction Detector ✅
**File:** `api/mutual_funds/dedup_detector.php`
**Migration:** `database/migrations/t479_migration.sql`

**DB changes:**
- `mf_transactions` — adds `dedup_hash`, `is_duplicate`, `duplicate_of`
- `stock_transactions` — adds `dedup_hash`, `is_duplicate`, `duplicate_of`
- `dedup_review_log` — new table for merge/dismiss audit

**Hash logic:**
- MF: `SHA256(portfolio_id|fund_id|date|type|units|amount)` — units tolerance ±0.01, amount ±₹1
- Stocks: `SHA256(portfolio_id|stock_id|date|type|qty|price)` — qty tolerance ±0.001, price ±0.01

**Actions implemented:**
| Action | Description |
|--------|-------------|
| `dedup_scan` | Full portfolio scan — writes hashes + flags duplicates |
| `dedup_list` | List flagged duplicates grouped by hash |
| `dedup_merge` | Mark one txn as dup of another (with audit) |
| `dedup_dismiss` | Clear false-positive flag |
| `dedup_check_new` | Pre-insert check before mf_add/stocks_add |
| `dedup_stats` | Count of open duplicates per asset type |

---

## t480 — Data Validation Rules ✅
**File:** `api/mutual_funds/data_validation.php`
**Migration:** `database/migrations/t480_migration.sql`

**Tables created:**
- `validation_rules` — configurable rules (min/max/regex/enum/date checks)
- `validation_violations` — scan results, per-entity violation log

**Seeded rules (22 default rules):**
- MF: units, nav, amount (positive + max), txn_date (past), txn_type (enum)
- Stocks: quantity, price (positive + max), txn_date, txn_type, brokerage (min 0)
- FD: principal, interest_rate (0.01–25%), maturity_date (future)
- NPS: units, nav, amount (positive)
- Savings: balance (min 0), interest_rate (max 20%)

**Actions implemented:**
| Action | Description |
|--------|-------------|
| `validate_entry` | Pre-insert validation — returns errors array |
| `validation_rules_list` | List rules (filter by asset_type) |
| `validation_rule_save` | Admin: create/update rule |
| `validation_rule_toggle` | Admin: enable/disable rule |
| `validation_rule_delete` | Admin: delete custom rule |
| `validation_scan` | Scan existing data, write violations to DB |
| `validation_violations` | List open violations for portfolio |
| `validation_resolve` | Mark violation as resolved |

**Integration point:** Call `validate_entry` before mf_add, stocks_add etc.
Example: `POST action=validate_entry&asset_type=mf&fields[units]=100&fields[nav]=...`

---

## t504 — WealthDash REST API ✅
**Files:**
- `api/rest/v1/index.php` — REST entry point (standalone, NOT via router)
- `api/rest/middleware.php` — Bearer auth + rate limiting (included by v1/index.php)
- `api/rest/api_key_manager.php` — Key CRUD (via router.php)

**Migration:** `database/migrations/t504_migration.sql`

**Tables created:**
- `api_keys` — hashed keys, scopes, rate limits, prefix for display
- `api_request_log` — per-request log (method, endpoint, status, response_ms)
- `api_rate_limit` — per-minute sliding window counter

**REST Endpoints (GET only, Bearer auth):**
| Endpoint | Scope | Description |
|----------|-------|-------------|
| `GET /api/rest/v1/portfolio` | portfolio:read | Net worth + breakdown |
| `GET /api/rest/v1/holdings` | portfolio:read | All asset holdings |
| `GET /api/rest/v1/holdings/mf` | portfolio:read | MF holdings |
| `GET /api/rest/v1/holdings/stocks` | portfolio:read | Stock holdings |
| `GET /api/rest/v1/holdings/fd` | portfolio:read | FD accounts |
| `GET /api/rest/v1/holdings/nps` | portfolio:read | NPS holdings |
| `GET /api/rest/v1/holdings/savings` | portfolio:read | Savings accounts |
| `GET /api/rest/v1/transactions?limit=100&since=2025-01-01` | transactions:read | All txns |
| `GET /api/rest/v1/transactions/mf` | transactions:read | MF txns |
| `GET /api/rest/v1/transactions/stocks` | transactions:read | Stock txns |
| `GET /api/rest/v1/net-worth` | net_worth:read | NW + 12-month snapshots |

**Key Management Actions (via router):**
| Action | Description |
|--------|-------------|
| `api_keys_list` | List user's keys (masked, prefix only) |
| `api_key_create` | Generate key — shown ONCE in plaintext |
| `api_key_delete` | Revoke + delete key |
| `api_key_toggle` | Enable/disable without deleting |
| `api_usage_stats` | Per-day request stats (last 30 days) |

**Security:**
- Keys stored as SHA-256 hash (never plaintext in DB)
- Rate limit: 60 req/min default (configurable per key, max 1000)
- Sliding window per-minute, auto-purge old windows
- Max 10 active keys per user
- CORS headers included
- All scopes: `portfolio:read`, `transactions:read`, `net_worth:read`, `holdings:read`, `all:read`

---

## ROUTER NOTES FILE
`ROUTER_NOTES_IDM.txt` — Master yahan se exact copy-paste karega.
Full switch-case blocks + csrfExempt entries sab included hain.

## SERVER SETUP NEEDED (Master kare)
REST API v1 ke liye .htaccess ya nginx rule chahiye:

**.htaccess** (`/api/rest/v1/.htaccess`):
```
Options -Indexes
RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^ index.php [QSA,L]
```

**Nginx:**
```
location /api/rest/v1/ {
    try_files $uri /api/rest/v1/index.php?$args;
}
```
