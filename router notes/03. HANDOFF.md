# WealthDash — HANDOFF SUMMARY
Worker: ID-W1 | Session: Continuation
Date: 2026-05-14
Tasks: t115, t120, t144
Status: ALL COMPLETE ✅

---

## t115 — REITs & InvITs

### Files
| File | Path |
|------|------|
| Migration | `database/migrations/t115_migration.sql` |
| API | `api/reits/reits.php` |
| Page | `templates/pages/reits_invits.php` |
| JS | `public/js/reits.js` |

### DB Tables Created
- `reits_invits` — holdings per user/portfolio
- `reits_invits_transactions` — BUY/SELL per holding
- `reits_invits_distributions` — dividend/SPD/interest income
- `reits_invits_master` — pre-seeded with 9 known REITs/InvITs (Embassy, Mindspace, Brookfield, Nexus, PowerGrid, IndiGrid, IRB, NHAI, Bharat)

### Routes to add in api/router.php
```php
// t115 - REITs & InvITs
case 'reits_list':
case 'reits_summary':
case 'reits_add':
case 'reits_edit':
case 'reits_delete':
case 'reits_txn_add':
case 'reits_txn_list':
case 'reits_txn_delete':
case 'reits_dist_add':
case 'reits_dist_list':
case 'reits_dist_delete':
case 'reits_price_refresh':
case 'reits_master_search':
    require_once __DIR__ . '/reits/reits.php';
    break;
```

### Features
- Holdings CRUD with trust type (REIT/InvIT)
- Per-holding transaction history (BUY/SELL)
- Distribution income tracker (dividend, interest, SPD, return_of_capital) with TDS
- Manual price refresh → auto gain/loss/pct recalc
- Master symbol search from pre-seeded list
- Portfolio-level summary stats

---

## t120 — Smallcase Portfolio Sync

### Files
| File | Path |
|------|------|
| Migration | `database/migrations/t120_migration.sql` |
| API | `api/smallcase/smallcase.php` |
| Page | `templates/pages/smallcase.php` |
| JS | `public/js/smallcase.js` |

### DB Tables Created
- `smallcase_portfolios` — basket-level record
- `smallcase_holdings` — individual stocks per basket
- `smallcase_transactions` — invest/redeem/rebalance/dividend
- `smallcase_rebalance_history` — rebalance log with added/removed stocks

### Routes to add in api/router.php
```php
// t120 - Smallcase
case 'smallcase_list':
case 'smallcase_summary':
case 'smallcase_add':
case 'smallcase_edit':
case 'smallcase_delete':
case 'smallcase_holding_list':
case 'smallcase_holding_save':
case 'smallcase_holding_delete':
case 'smallcase_holding_bulk_import':
case 'smallcase_txn_list':
case 'smallcase_txn_add':
case 'smallcase_txn_delete':
case 'smallcase_rebalance_add':
case 'smallcase_rebalance_list':
case 'smallcase_price_update':
case 'smallcase_calc_xirr':
    require_once __DIR__ . '/smallcase/smallcase.php';
    break;
```

### Features
- Multiple smallcase baskets per portfolio
- Card-grid UI with GL and XIRR per basket
- Stock-level holdings with weight% and target weight%
- Bulk CSV import (paste: symbol, company, qty, avg_price, sector)
- Transaction tracker (invest/redeem/rebalance/dividend)
- Rebalance history log (stocks added/removed/changed)
- XIRR calculation using Newton-Raphson method
- Auto recalc of portfolio totals when holdings change

---

## t144 — SIP Step-Up Nudge

### Files
| File | Path |
|------|------|
| Migration | `database/migrations/t144_migration.sql` |
| API | `api/sip/sip_stepup.php` |
| Page | `templates/pages/sip_stepup.php` |
| JS | `public/js/sip_stepup.js` |

### DB Tables Created
- `sip_stepup_config` — step-up rules per SIP (type, value, frequency, month, cap)
- `sip_stepup_history` — audit log of every step-up applied
- `sip_stepup_nudges` — dismissable nudge alerts (salary_hike, fy_start, anniversary)

### Routes to add in api/router.php
```php
// t144 - SIP Step-Up
case 'stepup_list':
case 'stepup_save':
case 'stepup_delete':
case 'stepup_history':
case 'stepup_preview':
case 'stepup_apply':
case 'stepup_simulate':
case 'stepup_nudges':
case 'stepup_nudge_dismiss':
case 'stepup_nudge_salary_hike':
    require_once __DIR__ . '/sip/sip_stepup.php';
    break;
```

### Features
- Percentage OR fixed-amount step-up per SIP
- Frequency: yearly / half-yearly / custom interval
- Configurable apply month (default: April = FY start)
- Max SIP cap to prevent over-stepping
- "Got a Hike?" modal → apply all pending step-ups in one click
- Overdue banner with one-click apply
- 10-year projection simulator with XIRR-based corpus estimate
- Full history log (old amount → new amount, date, applied_by)
- Salary hike nudge persisted to DB for future dashboard widget

---

## Existing Files — NOT Modified
- `api/router.php` ← DO NOT TOUCH (routes noted above)
- `config/database.php`
- All other existing task files

## Next Steps for Next Worker
1. Run all 3 migrations in order: t115 → t120 → t144
2. Add the route blocks above into `api/router.php`
3. Add nav links for new pages in sidebar template
4. Test: reits_master_search uses `reits_invits_master` — seed data included in migration
5. t144 `stepup_simulate` depends on `sip_schedules` table existing — confirm schema match
