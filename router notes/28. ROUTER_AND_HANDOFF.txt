========================================================
ROUTER NOTES — ID-W5 Session 8 (6 Tasks)
t461, t462, t463, t471, tp004, tp005
========================================================

── t461: ULIP Tracker Full ──
  csrfExempt: 'ulip_fund_list','ulip_fund_history','ulip_performance'
  case 'ulip_fund_list':
  case 'ulip_fund_add':
  case 'ulip_fund_delete':
  case 'ulip_fund_history':
  case 'ulip_switch':
  case 'ulip_performance':
      require APP_ROOT . '/api/insurance/ulip_full.php'; exit;

── t462: Premium Calendar ──
  csrfExempt: 'premium_calendar_get','premium_history_list'
  case 'premium_calendar_get':
  case 'premium_mark_paid':
  case 'premium_history_list':
  case 'premium_payment_delete':
      require APP_ROOT . '/api/insurance/premium_calendar.php'; exit;

── t463: Property Portfolio ──
  csrfExempt: 'property_list','property_summary','property_valuation_history'
  case 'property_list':
  case 'property_add':
  case 'property_update':
  case 'property_delete':
  case 'property_summary':
  case 'property_valuation_add':
  case 'property_valuation_history':
      require APP_ROOT . '/api/property/property.php'; exit;

── t471: Monthly Budget Tracker ──
  csrfExempt: 'budget_get','budget_actual_list','budget_summary','budget_categories_list'
  case 'budget_get':
  case 'budget_save':
  case 'budget_actual_list':
  case 'budget_actual_add':
  case 'budget_actual_update':
  case 'budget_actual_delete':
  case 'budget_summary':
  case 'budget_categories_list':
      require APP_ROOT . '/api/budget/budget.php'; exit;

── tp004: PHP OPcache ──
  No API routes — config files only.

── tp005: Frontend Bundle ──
  No API routes — CLI build script only.

========================================================
SIDEBAR NOTES (templates/sidebar.php):
  Insurance section:
    'ulip_detail'         => pages/insurance/ulip_detail.php  (linked from ulip list)
    'premium_calendar'    => pages/insurance/premium_calendar.php
  Property section (NEW):
    'property'            => pages/property/property.php
  Budget section (NEW):
    'budget'              => pages/budget/budget.php

========================================================
OVERWRITE RULES:
  ALL 6 tasks → NEW files only, no overwrites.
========================================================

MIGRATION ORDER:
  1. t461_migration.sql  → ulip_switches + index (depends on t323: ulip_policies, ulip_fund_values)
  2. t462_migration.sql  → premium_payments (depends on t122: insurance_policies)
  3. t463_migration.sql  → properties + property_valuations
  4. t471_migration.sql  → budget_plans + budget_actuals
  (tp004, tp005 — no migrations)

========================================================
ADDITIONAL SETUP STEPS:
  tp004:
    1. Copy config/opcache.ini content into php.ini or .user.ini
    2. Add to index.php bootstrap: require_once APP_ROOT.'/config/opcache.php';
    3. Restart PHP-FPM: sudo systemctl restart php8.2-fpm
    4. Verify in admin: System Health page shows OPcache status

  tp005:
    1. Place build/build.php in project root's build/ folder
    2. Run from project root: php build/build.php
    3. layout.php already auto-serves .min.* via wd_js_url() (Session 4 tp005 logic)
    4. Run on every code deploy (or add to CI/CD)
    5. Watch mode: php build/build.php --watch
    6. Clean all minified: php build/build.php --clean

========================================================
COMBINED HANDOFF SUMMARY — ID-W5 Session 8 (6 Tasks)
========================================================

✅ t461 — ULIP Tracker Full (Fund-level allocation + switching)
   api/insurance/ulip_full.php + pages/insurance/ulip_detail.php + migration
   DB: ulip_switches (new) + index on ulip_fund_values
   Notes:
     - Extends t323 (basic ULIP CRUD) with per-fund NAV snapshot tracking
     - Fund-level allocation doughnut chart + value history line chart
     - Fund switch: moves ₹ amount from one fund to another within same policy
     - Auto-recalculates ulip_policies.current_fund_value on every add/switch
     - Access via: ?page=ulip_detail&id=<ulip_id> (linked from t323 list page)

✅ t462 — Premium Calendar (Due dates + payment history)
   api/insurance/premium_calendar.php + pages/insurance/premium_calendar.php + migration
   DB: premium_payments (with unique constraint on user_id+policy_id+due_date)
   Notes:
     - Year-view 12-month grid with all due dates auto-generated from
       next_premium_date + premium_frequency (monthly/quarterly/half-yearly/annual)
     - Mark paid → records payment method + advances next_premium_date on policy
     - Payment history modal with delete/undo
     - Annual total shown in header

✅ t463 — Property Portfolio (Real estate management)
   api/property/property.php + pages/property/property.php + migration
   DB: properties + property_valuations (2 new tables)
   Notes:
     - CRUD for 5 property types: residential/commercial/land/agricultural/other
     - Valuation history — multiple valuations per property (manual/agent/govt circle/registered)
     - Auto-seeds first valuation = purchase price on add
     - Calculated: equity (value - loan), gain, gain_pct, CAGR, rental yield
     - Rental yield = (monthly_rental × 12) / current_value × 100

✅ t471 — Monthly Budget Tracker (Income vs Outflow)
   api/budget/budget.php + pages/budget/budget.php + migration
   DB: budget_plans + budget_actuals (2 new tables)
   Notes:
     - 18 pre-built categories (4 income, 13 expense, 1 savings) with icons/colors
     - Two-panel UI: plan budget (set target amounts per category) + record actuals
     - Monthly navigation with prev/next buttons + month picker
     - Overview tab: horizontal progress bars, actual vs budget with over-budget warning
     - Transactions tab: full list with edit/delete
     - Savings rate = (income - expense) / income × 100
     - Budget plan stored as JSON key-value (category → amount) per month

✅ tp004 — PHP OPcache Configuration
   config/opcache.php + config/opcache.ini
   DB: No changes
   Notes:
     - opcache.php: runtime check helper, wd_opcache_status(), wd_opcache_reset()
     - opcache.ini: recommended settings for production (JIT enabled, validate_timestamps=0)
     - Add to System Health admin page: wd_opcache_status() already integrated
     - Dev environment: set validate_timestamps=1, revalidate_freq=2

✅ tp005 — Frontend Bundle (Minify CSS/JS)
   build/build.php (CLI script)
   DB: No changes
   Notes:
     - Pure PHP minifier (no npm/webpack required)
     - CSS: removes comments, collapses whitespace, removes redundant semicolons
     - JS: removes single + multi-line comments, collapses whitespace, preserves
       strings (single/double/template), preserves /*! licence comments
     - Watch mode: checks file mtimes every 2s, re-builds on change
     - layout.php already serves .min.* via wd_js_url() (existing tp005 logic)
     - Run on deploy: php build/build.php
     - Expected savings: ~30-50% file size reduction

========================================================
