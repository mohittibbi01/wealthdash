-- ============================================================
-- WealthDash — t420: Deployment Guide & Docker
-- Migration: database/migrations/t420_migration.sql
-- Worker: ID-M
-- NOTE: Docker/deploy files are config only — no schema changes.
-- ============================================================
SELECT 'Deployment Guide — no schema changes required' AS note;
