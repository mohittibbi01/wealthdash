-- WealthDash — tp004: PHP OPcache Migration
-- No database changes required.
-- Files: config/opcache.php (include in bootstrap), config/opcache.ini (copy to php.ini or .user.ini)
-- After placing opcache.ini: restart PHP-FPM (sudo systemctl restart php8.2-fpm)
SELECT 1;
